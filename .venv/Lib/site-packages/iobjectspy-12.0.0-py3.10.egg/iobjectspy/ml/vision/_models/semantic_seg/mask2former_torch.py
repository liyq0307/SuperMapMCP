from .api import SegTorchTrainer
from .api import SegTorchEstimation
from ._torch_models import build_torch_seg_model


class Mask2FormerTrainer(SegTorchTrainer):

    def get_loss_type(self):
        self.loss_type = 'maskcls_loss'


class Mask2FormerEstimation(SegTorchEstimation):
    def __init__(self, model_path, config, gpus, batch_size, test_aug=False, **kwargs):
        super(Mask2FormerEstimation, self).__init__(model_path, config, gpus, batch_size, test_aug, **kwargs)
        pass
